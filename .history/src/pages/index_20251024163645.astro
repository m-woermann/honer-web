---
// Simple landing page
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Horner Audio</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Michroma&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: #383e42; /* RAL 7016 Anthracite Grey */
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .landing-container {
            text-align: center;
            padding: 2rem;
            max-width: 800px;
        }

        h1 {
            font-family: 'Michroma', sans-serif;
            font-size: 4rem;
            font-weight: 400;
            color: #42cc5d;
            margin-bottom: 2rem;
            text-shadow: 0 0 30px rgba(66, 204, 93, 0.4);
            letter-spacing: 4px;
            animation: fadeInUp 1s ease-out;
        }

        p {
            font-size: 1.2rem;
            color: #ccc;
            margin-bottom: 3rem;
            animation: fadeInUp 1s ease-out 0.2s both;
        }

        .nav-buttons {
            display: flex;
            gap: 2rem;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease-out 0.4s both;
        }

        .nav-button {
            display: inline-block;
            padding: 1.2rem 3rem;
            background: transparent;
            border: 2px solid rgba(66, 204, 93, 0.3);
            color: #42cc5d;
            text-decoration: none;
            font-family: 'Michroma', sans-serif;
            font-size: 1rem;
            letter-spacing: 2px;
            border-radius: 4px;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .nav-button:hover {
            background: rgba(66, 204, 93, 0.1);
            border-color: rgba(66, 204, 93, 0.6);
            transform: translateY(-4px);
            box-shadow: 0 8px 20px rgba(66, 204, 93, 0.2);
        }

        .nav-button.primary {
            background: rgba(66, 204, 93, 0.1);
            border-color: rgba(66, 204, 93, 0.5);
        }

        .nav-button.primary:hover {
            background: rgba(66, 204, 93, 0.2);
            border-color: #42cc5d;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2.5rem;
                letter-spacing: 2px;
            }

            p {
                font-size: 1rem;
            }

            .nav-buttons {
                flex-direction: column;
                gap: 1rem;
            }

            .nav-button {
                padding: 1rem 2rem;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="landing-container">
        <h1>Horner Audio</h1>
        <p>Professional Audio Systems Visualization</p>
        
        <div class="nav-buttons">
            <a href="/home" class="nav-button primary">3D Viewer</a>
            <a href="/products" class="nav-button">Products</a>
        </div>
    </div>
</body>
</html>